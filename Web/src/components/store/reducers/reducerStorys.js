const reducerStoryInitialState = {};
const reducerStorys = (state = reducerStoryInitialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default reducerStorys;
